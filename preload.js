// The renderer (index.html/app.js) only uses standard web APIs
// (MediaRecorder, Web Audio, IndexedDB) and needs no Node/Electron
// APIs exposed, so this preload script intentionally does nothing.
